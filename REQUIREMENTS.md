# Virtual environment
The virtual environment based on Python 3.12.6

# Python packages
See in requirements.txt
To install the easily, run 'pip install -r requirements.txt' command in the terminal (while you are in the venv created for the project)

# Data files
